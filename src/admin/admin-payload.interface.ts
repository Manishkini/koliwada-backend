export interface AdminPayload {
    id: string
    firstName: string
    middleName: string
    lastName: string
    role: string
    mobileNumber: string
    email: string
    permissions: string[]
}